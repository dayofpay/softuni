/// <reference types="jest" />
/// <reference types="node" />
import { EventEmitter } from 'events';
import { LocationMock } from '@jedmao/location';
declare class WindowMock extends EventEmitter {
    addEventListener: jest.Mock<this, [string, (...args: unknown[]) => void]>;
    removeEventListener: jest.Mock<this, [string, (...args: unknown[]) => void]>;
    location: LocationMock;
    history: {
        pushState: () => void;
    };
    mockClear(): void;
}
export default WindowMock;
