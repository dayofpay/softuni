function start(size) {
    const line = "* ".repeat(size).trim();
  
    for (let i = 0; i < size; i++) {
      console.log(line);
   
    }

    // let one = "";
    // for(let x = 1;x<=size;x++){
    //     one+="* ";
    // }
    // // while(one.includes(" ")){
    // //     one = one.replace(' ','');
    // // }
    // for(let b = 1; b<= size;b++){
    //     console.log(one);
    // }
  }
start(7);